---
name: writing-bash-tests
description: Use when authoring or running bash tests with crucible — files ending in _test.sh holding test_* functions, invoked via crucible.sh. Crucible is the tiny one-file bash test runner this plugin ships; the skill teaches its conventions, assertions, and the run helper for stdout/stderr capture.
---

# Writing Bash Tests with Crucible

Crucible is a single-file bash test runner. This plugin ships the runner (`crucible.sh`) and this skill, which encodes the conventions for authoring tests against it.

## Philosophy

*(Body authored in a follow-up commit. See the README and `crucible.sh`'s header comment for the canonical conventions, assertion list, and `run` helper documentation in the meantime.)*

## Guidance

*(Body authored in a follow-up commit.)*

## Validation

*(Body authored in a follow-up commit.)*
